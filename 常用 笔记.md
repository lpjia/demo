临时起意的东西, 如果超过一个月就可以迁移到这里

得添加时间来记录当时时间



--------以上内容置顶--------

倒序, 占位


### 命名 单复数问题

通用的做法是单个用单数, 多个用复数
不区分数据的类型, 只看数据的个数或字段的个数, 或者叫成员的个数
比如
``` javascript
const doms = {
  btn: document.querySelector('.btn'),
  container: document.querySelector('.container'),
}
// doms有2个字段
```


用单数吧, 保持一致性

英文的复数形式很多, 不好记, 中国人的英文水平不一致, 容易带来困扰

分几种命名
* 文件夹
* 变量
* 方法、函数


单数+List/Map/Set/Array

#### 文件夹命名

单数

#### 变量命名

> 单数

> 复数, 不同的数据类型用对应的简写结尾, 以js为例

``` javascript
const dom = {
  btn: document.querySelector('.btn'),
  container: document.querySelector('.container'),
}

const shopObj = {
  id: 111,
  name: '京小盒',
  position: '大街上',
}

const shopArr = ['京小盒', '丹尼斯', '胖东来']

const shopMap = new Map([
  [shopObj, '这是shopObj的值'],
  [shopArr, '这是shopArr的值'],
])
```
