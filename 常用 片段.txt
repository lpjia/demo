





d:\demo\* folder: *关键词* !d:\demo\*\*

d:\demo\* *关键词*



node_modules,lib,*-lock.*,*.map




import fs from 'node:fs/promises'
import path from 'node:path'
import { fileURLToPath } from 'node:url';

const __dirname = fileURLToPath(new URL('.', import.meta.url));
const filePath = path.resolve(__dirname, '拿路径去拼接')
const file = await fs.readFile(filePath, 'utf-8')
// file拿到的是文件内容, 一般是字符串; 有些比如json文件, 想当对象用, 得解析 反序列化 JSON.parse(file)




把这个会话的聊天记录write到根目录的1个文件


识别并转文本

识别并转文本
不要中文词汇

等会把我发的图片
识别并转文本

识别并转文本
翻译成中文



Claude Code



"开发依赖"
"仅仅生产依赖"


例子看demo搜
ramda



export function App(){
  return (
    <>
    </>
  )
}

export default App

import {} from 'abc'
import type {} from 'abc'
import ABC from 'abc'


export default (param) => {
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve(param)
    }, 3000);
  });
}


function sleep(delay) {
  return new Promise((resolve) => {
    setTimeout(resolve, delay)
  })
}
await sleep(2000)



let resolve, reject;
const promise = new Promise((res, rej) => {
  resolve = res;
  reject = rej;
});




console.time('标记')
console.timeEnd('标记')



浏览器打印台使用
console.log('---- 分隔线 ----\n\n\n')
console.log('---- 分隔线 111 ----\n\n\n')
111到999, 够用了

终端使用
console.log('---- 分隔线 ----\n')

vscode 输出使用
console.log('\n')



----以下为旧文件内容----
----以下为之前的----


--------以上置顶--------
--------
----以上内容置顶----

--------分割线--------


<p>----分隔线----</p>

<el-divider></el-divider>


--------2025-09-29 16:12--------
--------2025-09-29 16:13 end--------


z_software

md_imgs


Ctrl
Shift
Ctrl Shift
Alt
Alt Shift


<%=  %>


this.$nextTick(() => {

})


、
  中文逗号
。
  中文句号
（）
  中文括号
——
  中文双横线
→
 → 
  前后各加了空格, 美观



测试实参有几个
handle(...args){
  console.log('args: ', args)
}
回调, 直接拿过去用
function (...args){
  console.log('args: ', args)
}



win10 开机启动地址
C:\ProgramData\Microsoft\Windows\Start Menu\Programs\StartUp



let curTime = dayjs().format('YYYY-MM-DD HH:mm:ss')


$wh: 50px;
width: $wh;
height: $wh;

@mixin wh($px) {
  width: $px;
  height: $px;
}

@include wh(30px);
