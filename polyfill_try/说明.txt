




语言问题
  兼容性
    API兼容   使用polyfill, 一般常用core-js库
    语法兼容  syntax transformer 语法转换 ( runtime )
      不同的语法用不同的工具来转换(没有一个大而全的工具)
  语法增强    babel   AST
    源代码 --> AST --> (插件, 来影响转换结果) --> 目标代码

    babel 预设preset (一堆插件) @babel/preset-env
    swc(rust)