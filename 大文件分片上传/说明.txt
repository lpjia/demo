2026-05-05 20:03
前端大文件分片
比较耗时的不是IO操作, 而是计算hash, 这是个cpu密集型操作


navigator.hardwareConcurrency
获取硬件的核心数, 常用于优化Web Worker并发数
用这个作为并发线程数


cutFile2.js 作为对比
这是没有加线程


不同线程完成任务的先后不一样, 所以用下标来存各自返回的分片list
result[i] = e.data
[
  [分片, 分片, ...],
  [...],
  [...]
]
