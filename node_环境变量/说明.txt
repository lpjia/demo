2026-04-07 23:55
node index.js
直接执行则 process.env.NODE_ENV 是 undefined
说明 process.env 原生没有这个属性, 是另外的东西注入的

if (process.env.NODE_ENV === 'production')
返回 false

不用 cross-env, 一般情况下 if (process.env.NODE_ENV === 'production') 是符合预期的执行逻辑



以后加上 cross-env 包 -D
cross-env NODE_ENV=production node index.js

NODE_ENV=啥, process.env.NODE_ENV的值就是啥

惯例是用 development、production
vite 又加了 staging

我记得之前在其他项目里见过 testing

和 dotenv 配合, 根据命令行(不同环境)加载相应环境下的变量值




dotenv 包
在文件最顶部引入 require('dotenv').config();
命令 node load_env_file.js
默认加载 .env 环境文件, 用path属性来加载其他环境文件




node原生支持 --env-file (Node.js v20.6.0+) 和 --env-file-if-exists (Node.js v22.9.0+) 可以作为命令行标志传递，以从指定的文件中加载环境变量
--env-file 如果文件缺失，则会抛出异常。如果文件可能不存在，请使用 --env-file-if-exists

node --env-file=.env no_dotenv.js
node --env-file=.env.development no_dotenv.js
node --env-file=.env.production no_dotenv.js