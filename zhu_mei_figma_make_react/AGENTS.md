# 改动代码要遵循这些原则:

1. 绝对不能修改或删除已经被注释掉的代码;
2. 如果需要改动已有的代码，先注释掉已有代码，再新增代码;
3. 新增代码时，在代码顶部用注释说明为什么要这么修改;

# 改好代码后

1. 不要执行编译命令, 如果需要的话提示我, 我自己手动执行编译命令;
2. 也不要执行"验证代码结果是否正确"的命令, 我自己会验证结果;

# 使用npm还是pnpm

1. 如果需要使用npm命令的话, 用pnpm命令来代替;
2. 如果需要安装npm包的话, 不要执行安装命令, 提示我, 我自己手动执行安装命令;
