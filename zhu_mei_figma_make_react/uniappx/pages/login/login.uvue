<template>
	<view class="page">
		<view class="brand-area">
			<view class="brand-icon"><text class="brand-symbol">⌂</text></view>
			<text class="brand-title">工地管理系统</text>
			<text class="brand-subtitle">登录您的账户以继续</text>
		</view>
		<view class="form-area">
			<view class="input-shell">
				<text class="input-icon">◉</text>
				<input class="login-input" v-model="phone" type="number" maxlength="11" placeholder="请输入手机号" />
			</view>
			<view class="input-shell">
				<text class="input-icon">□</text>
				<input class="login-input" v-model="password" :password="!showPassword" placeholder="请输入密码" />
				<view class="password-toggle" @click="showPassword = !showPassword"><text class="toggle-text">{{ showPassword ? '隐藏' : '显示' }}</text></view>
			</view>
			<button class="login-button" @click="login">登录</button>
		</view>
	</view>
</template>

<script setup lang="uts">
	const phone = ref('')
	const password = ref('')
	const showPassword = ref(false)

	function login(): void {
		uni.switchTab({ url: '/pages/overview/overview' })
	}
</script>

<style>
	.page { flex: 1; min-height: 100%; background-color: #FFFFFF; }
	.brand-area { padding: 150rpx 48rpx 92rpx; display: flex; flex-direction: column; align-items: center; background-image: linear-gradient(to bottom, #FFF7ED, #FFFFFF); }
	.brand-icon { width: 112rpx; height: 112rpx; display: flex; align-items: center; justify-content: center; background-color: #FFFFFF; border: 1px solid #FED7AA; border-radius: 28rpx; }
	.brand-symbol { color: #F97316; font-size: 62rpx; line-height: 62rpx; }
	.brand-title { margin-top: 32rpx; color: #111827; font-size: 44rpx; font-weight: 700; }
	.brand-subtitle { margin-top: 12rpx; color: #9CA3AF; font-size: 26rpx; }
	.form-area { padding: 0 48rpx; }
	.input-shell { height: 96rpx; display: flex; flex-direction: row; align-items: center; margin-bottom: 24rpx; padding: 0 28rpx; background-color: #F8F9FB; border: 1px solid #D1D5DB; border-radius: 24rpx; }
	.input-icon { width: 38rpx; color: #9CA3AF; font-size: 28rpx; }
	.login-input { flex: 1; height: 96rpx; margin-left: 18rpx; color: #111827; font-size: 28rpx; }
	.password-toggle { padding: 20rpx 0 20rpx 20rpx; }
	.toggle-text { color: #F97316; font-size: 24rpx; font-weight: 500; }
	.login-button { height: 96rpx; margin-top: 16rpx; color: #FFFFFF; background-color: #F97316; border-radius: 24rpx; font-size: 30rpx; font-weight: 600; line-height: 96rpx; }
</style>
