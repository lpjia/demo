
// 2025-06-04 10:19 星期三
### FormData 上传文件.html
重新总结, 对表单重新加深了认识, 推荐form包裹表单元素



// 2023-10-28 21:53 星期六

### 上传文件 原生file.html
这个html用来调试上传, 并没有实现上传功能, 只是调试了上传的交互以及会触发什么事件


### 原生file 用form action上传.html
通过form action属性直接提交上传文件
完成了上传功能
没有用到js代码, 纯html实现, 不存在跨域问题
虽然没有跨域问题, 但其实是"跨域了", 前端页面是http://127.0.0.1:8848, 后端服务是http://localhost:7010
``` javascript
这个方案关键点是action属性 target="iframe" enctype="multipart/form-data" 
<iframe id="frame" name="iframe" style="display:none;"></iframe>
如果没有这些关键, 调试发现有默认跳转页面行为
```


### koa2_ts_mysql项目还有一个html
index.html放到/static/upload目录下
访问路径是http://127.0.0.1:7010/upload/index.html
调试不跨域的情况下, 通过a标签和download属性实现下载功能




// 2023-10-31 13:54 星期二

### 原生form action 提交后阻止页面跳转.html
完美解决方案


----以上是用form元素、action属性和method属性, 没有显式使用ajax----


### 原生form formData加axios.html
const formData = new FormData(doms.form) // 创建一个formData实例

GET请求
new URLSearchParams(formData)创建一个实例
const formDataQuery = new URLSearchParams(formData)
formDataQuery就可以直接传给后端了
如果前端想拿到obj结构的数据
const frontendData = Qs.parse(formDataQuery.toString())

POST等复杂请求
formData就可以直接传给后端了

两种请求都不需要手动配置请求头的Content-Type

axios和qs在浏览器环境, 挂载到window, 分别是window.axios和window.Qs